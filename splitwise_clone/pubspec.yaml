name: splitwise_clone
description: Splitwise clone app for diploma project
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Firebase
  firebase_core: ^2.24.2
  firebase_auth: ^4.15.3
  cloud_firestore: ^4.13.6
  firebase_messaging: ^14.7.9
  google_sign_in: ^6.2.1
  
  # State Management
  provider: ^6.1.1
  
  # UI и утилиты
  fl_chart: ^0.66.0
  flutter_local_notifications: ^19.2.1
  intl: ^0.19.0
  
  # Дополнительные пакеты
  cached_network_image: ^3.3.1
  uuid: ^4.2.2
  shared_preferences: ^2.2.2
  connectivity_plus: ^5.0.2
  permission_handler: ^11.1.0
  path_provider: ^2.1.1
  
  # UI компоненты
  flutter_slidable: ^3.0.1
  shimmer: ^3.0.0
  animations: ^2.0.11

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true